const { parseBoundedInt } = require('../utils/number');

module.exports = {
  DEFAULT_CAMPAIGN_DAILY_BUDGET: 300000,
  SHOPEE_CAMPAIGN_DAILY_BUDGET: 50000,
  SHOPEE_AD_SET_BID_AMOUNT: 500,
  SHOPEE_AGE_MIN: 20,
  SHOPEE_AGE_MAX: 44,
  DEFAULT_AD_SET_NAME: 'Nh\u00f3m qu\u1ea3ng c\u00e1o L\u01b0\u1ee3t mua m\u1edbi',
  DEFAULT_AD_NAME_PREFIX: 'PHAT',
  DEFAULT_POST_LABEL_PREFIX: 'MS',
  DEFAULT_CAMPAIGN_OBJECTIVE: 'OUTCOME_ENGAGEMENT',
  DEFAULT_CAMPAIGN_BID_STRATEGY: 'LOWEST_COST_WITHOUT_CAP',
  VN_OFFSET_MS: 7 * 60 * 60 * 1000,
  SHOPEE_CAMPAIGN_BID_STRATEGY: 'COST_CAP',
  DEFAULT_AD_SET_DESTINATION_TYPE: 'MESSENGER',
  DEFAULT_AD_SET_OPTIMIZATION_GOAL: 'MESSAGING_PURCHASE_CONVERSION',
  META_POST_REQUEST_LIMIT: 100,
  POSTS_PER_PAGE_LIMIT: 500,
  SHOPEE_POSTS_PER_PAGE_LIMIT: 500,
  ALL_POSTS_MAX_LIMIT: 50000,
  CAMPAIGN_CREATE_CONCURRENCY: parseBoundedInt(process.env.CAMPAIGN_CREATE_CONCURRENCY, 1, 1, 3),
  CAMPAIGN_CREATE_ITEM_DELAY_MS: parseBoundedInt(process.env.CAMPAIGN_CREATE_ITEM_DELAY_MS, 0, 0, 60000),
  CAMPAIGN_DUPLICATE_QUEUE_DELAY_MS: parseBoundedInt(process.env.CAMPAIGN_DUPLICATE_QUEUE_DELAY_MS, 1500, 0, 30000),
  CAMPAIGN_DUPLICATE_COPY_STATUS: 'PAUSED',
  FB_RATE_LIMIT_BACKOFF_MS: parseBoundedInt(process.env.FB_RATE_LIMIT_BACKOFF_MS, 60000, 5000, 300000),
  FB_RATE_LIMIT_RETRIES: parseBoundedInt(process.env.FB_RATE_LIMIT_RETRIES, 3, 1, 10),
  SHOPEE_AD_ACCOUNT_NAME_PATTERN: /^XK\s*(11|12|13)\b/i,
  FACEBOOK_GRAPH_API_VERSION: process.env.FB_GRAPH_API_VERSION || 'v24.0',
  FINAL_SPEND_CRON: process.env.FINAL_SPEND_CRON || '20 0 * * *',
  FINAL_SPEND_TIMEZONE: process.env.FINAL_SPEND_TIMEZONE || 'Asia/Ho_Chi_Minh',
  TODAY_CAMPAIGN_SYNC_INTERVAL_MS: parseBoundedInt(process.env.TODAY_CAMPAIGN_SYNC_INTERVAL_MS, 10 * 60 * 1000, 2 * 60 * 1000, 60 * 60 * 1000),
  REDIS_URL: String(process.env.REDIS_URL || process.env.BULLMQ_REDIS_URL || '').trim(),
  REDIS_QUEUE_ENABLED: ['1', 'true', 'yes'].includes(String(process.env.REDIS_QUEUE_ENABLED || '').trim().toLowerCase()) ||
    Boolean(String(process.env.REDIS_URL || process.env.BULLMQ_REDIS_URL || '').trim()),
  REDIS_HOST: process.env.REDIS_HOST || '127.0.0.1',
  REDIS_PORT: parseBoundedInt(process.env.REDIS_PORT, 6379, 1, 65535),
  REDIS_PASSWORD: process.env.REDIS_PASSWORD || undefined,
  CAMPAIGN_DUPLICATE_QUEUE_NAME: process.env.CAMPAIGN_DUPLICATE_QUEUE_NAME || 'campaign-duplicates',
  CAMPAIGN_DUPLICATE_QUEUE_CONCURRENCY: parseBoundedInt(process.env.CAMPAIGN_DUPLICATE_QUEUE_CONCURRENCY, 1, 1, 5),
  CAMPAIGN_DUPLICATE_JOB_ATTEMPTS: parseBoundedInt(process.env.CAMPAIGN_DUPLICATE_JOB_ATTEMPTS, 1, 1, 5),
  CAMPAIGN_DUPLICATE_JOB_BACKOFF_MS: parseBoundedInt(process.env.CAMPAIGN_DUPLICATE_JOB_BACKOFF_MS, 30000, 1000, 300000),
  CAMPAIGN_SYNC_QUEUE_NAME: process.env.CAMPAIGN_SYNC_QUEUE_NAME || 'campaign-sync-history',
  CAMPAIGN_SYNC_QUEUE_CONCURRENCY: parseBoundedInt(process.env.CAMPAIGN_SYNC_QUEUE_CONCURRENCY, 1, 1, 3),
  CAMPAIGN_SYNC_JOB_ATTEMPTS: parseBoundedInt(process.env.CAMPAIGN_SYNC_JOB_ATTEMPTS, 1, 1, 5),
  CAMPAIGN_SYNC_JOB_BACKOFF_MS: parseBoundedInt(process.env.CAMPAIGN_SYNC_JOB_BACKOFF_MS, 30000, 1000, 300000),
  CAMPAIGN_SYNC_DAY_DELAY_MS: parseBoundedInt(process.env.CAMPAIGN_SYNC_DAY_DELAY_MS, 300, 0, 10000),
  ORDER_SHEET_SYNC_QUEUE_NAME: process.env.ORDER_SHEET_SYNC_QUEUE_NAME || 'order-sheet-sync',
  ORDER_SHEET_SYNC_QUEUE_CONCURRENCY: parseBoundedInt(process.env.ORDER_SHEET_SYNC_QUEUE_CONCURRENCY, 1, 1, 3),
  ORDER_SHEET_SYNC_JOB_ATTEMPTS: parseBoundedInt(process.env.ORDER_SHEET_SYNC_JOB_ATTEMPTS, 2, 1, 5),
  ORDER_SHEET_SYNC_JOB_BACKOFF_MS: parseBoundedInt(process.env.ORDER_SHEET_SYNC_JOB_BACKOFF_MS, 30000, 1000, 300000)
};
